# Imported from the stats package

Imported from the stats package

## Usage

``` r
format_perc(
  x,
  digits = max(2L, getOption("digits")),
  probability = TRUE,
  use.fC = length(x) < 100,
  ...
)
```
